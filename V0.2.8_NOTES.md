# v0.2.8

- الطباعة أصبحت تُرسل من Electron نفسه بإعداد A4 Landscape بدل الاعتماد على CSS فقط.
- زر مستقل لحفظ PDF أفقي حقيقي عبر `webContents.printToPDF`.
- زر مستقل للطباعة الورقية A4 أفقي عبر `webContents.print`.
- الإبقاء على CSS `@page A4 landscape` كطبقة دعم إضافية.
