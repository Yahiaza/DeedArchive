# v0.2.2 - Excel range import

تم تغيير الاستيراد ليعمل بنطاق خلايا يدوي مثل برنامج القروض:

1. اختيار ملف Excel/CSV.
2. اختيار ورقة العمل.
3. تحديد أول خلية وآخر خلية، مثال: A3 إلى J84.
4. لا يتم إنشاء معاينة البيانات إلا بعد الضغط على «معاينة النطاق».
5. تتم قراءة الصفوف المطلوبة فقط باستخدام `sheetRows` ثم قص الأعمدة إلى النطاق المحدد.
6. بعد المعاينة يمكن تحديد صف العناوين وربط الأعمدة ثم الاستيراد.

حدود حماية: 10,000 صف و100 عمود لكل عملية استيراد.
